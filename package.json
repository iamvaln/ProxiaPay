{
  "name": "proxiapay",
  "private": true,
  "version": "0.1.0",
  "description": "ProxiaPay — mobile money payment platform for Central and West Africa",
  "workspaces": ["apps/api", "apps/console"],
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "npm run build -w apps/api && npm run build -w apps/console",
    "test": "npm run test -w apps/api",
    "lint": "npm run lint -w apps/api",
    "typecheck": "npm run typecheck -w apps/api && npm run typecheck -w apps/console",
    "migrate": "npm run migrate -w apps/api",
    "seed": "npm run seed -w apps/api",
    "dev:api": "npm run dev -w apps/api",
    "dev:worker": "npm run dev:worker -w apps/api",
    "dev:console": "npm run dev -w apps/console"
  }
}
